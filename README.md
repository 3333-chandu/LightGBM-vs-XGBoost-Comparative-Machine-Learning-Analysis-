# LightGBM-vs-XGBoost-Comparative-Machine-Learning-Analysis-
An example project comparing LightGBM and XGBoost models using the Titanic dataset. Includes complete workflow: data preprocessing, EDA, feature encoding, model training, hyperparameter tuning, and performance evaluation to identify which boosting algorithm performs better.
